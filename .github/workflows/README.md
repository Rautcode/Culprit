# .github/workflows

Path-filtered CI per service (lint → test → build → push to ECR), populated
at SPEC_VERSION.md "v1.0 Build Sequence" step 10 — deliberately not stubbed
with a fake passing workflow before there's real code and real tests to run.
See docs/09-infrastructure.md "Delivery".
