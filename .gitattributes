# Normalize line endings: LF in the repo, checked out natively per-OS.
# Kills the "LF will be replaced by CRLF" warnings on Windows.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
